import Vue from "vue";
import Vuetify from "vuetify/lib/framework";

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    light: {
      primary: "#FC8181",
      secondary: "#FEB2B2",
      accent: "#E53E3E",
    },
  },
});
